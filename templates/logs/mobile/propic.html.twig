<table id="log" class="table table-striped table-bordered">
    <thead>
        <tr>
		    <td>Utente</td>
		    <td>Data</td>
		    <td>Vecchia immagine profilo</td>
		    <td>Nuova immagine profilo</td>
        </tr>
    </thead>
    <tbody>
        {% for d in data %}
			<tr>
				<td class="centered">{{ d.source.login }}</td>
                <td class="centered">{{ d.date|date("d/m/Y h:i A") }}</td>
                <td class="centered"><img src="{{ '/images/propics/' ~ d.oldPropic }}"" width="50" height="50"></img></td>
				<td class="centered"><img src="{{ '/images/propics/' ~ d.newPropic }}"" width="50" height="50"></img></td>
			</tr>
	    {% endfor %}
    </tbody>
</table>
<script>
    $(document).ready(function() {
    $('#log').DataTable();
</script>